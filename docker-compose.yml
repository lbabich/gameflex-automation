services:
  app:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      DISPLAY: ":99"
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
    volumes:
      - ./src/core/data:/app/src/core/data
