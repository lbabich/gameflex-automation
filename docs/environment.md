# Environment Setup

Copy `.env.example` to `.env` and fill in all required variables before running tests.

| Variable | Description |
|---|---|
| `GAME_URL` | URL of the game to test |
| `ANTHROPIC_API_KEY` | API key for Claude Vision |
